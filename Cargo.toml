[package]
name = "qams"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
qams_core = { git = "https://github.com/finleyowen/qams_core", version = "0.1.0" }
